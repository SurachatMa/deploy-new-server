export enum EBillingMode {
  Hourly = 'hourly',
  Monthly = 'monthly',
}

export enum EAddonType {
  Ram = 'ram',
  Disk = 'disk',
}

export enum EServerType {
  VirtualMachine = 'virtual-machine',
  Dedicated = 'dedicated',
}
